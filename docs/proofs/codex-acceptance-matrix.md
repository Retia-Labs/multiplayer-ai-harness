# Codex acceptance result matrix

Generated by `node test/codex-acceptance.js` on 2026-09-05T18:19:49.581Z. Evidence for every row is in
[`codex-acceptance-result.json`](codex-acceptance-result.json); the provider's own event stream is in
[`codex-acceptance-provider.jsonl`](codex-acceptance-provider.jsonl).

- Host: win32/x64, Node v24.20.0
- Codex CLI: **0.142.1** (tested against 0.142.1), resolved as vendored
- Auth mode: **chatgpt**
- Lanes: control=executed, subscription=executed, api=executed
- Provider event lines captured: 23

| Criterion | Check | Result | What it showed |
| --- | --- | --- | --- |
| AC4 version & prerequisites | `codex-resolvable` | **PASS** | resolved the Codex binary as vendored at C:\Users\tkala\AppData\Roaming\npm\node_modules\@openai\codex\node_modules\@openai\codex-win32-x64\vendor\x86_64-pc-windows-msvc\bin\codex.exe |
| AC4 version & prerequisites | `version-pinned` | **PASS** | running the pinned, tested Codex 0.142.1 |
| AC4 version & prerequisites | `platform-prerequisites` | **PASS** | recorded the prerequisites this adapter depends on for this platform |
| AC4 version & prerequisites | `capability-gaps-have-an-answer` | **RECORDED** | 2 capability gap(s) recorded, each with a concrete alternative or blocker |
| AC3 authentication | `usage-owner-and-entitlements` | **RECORDED** | recorded who pays and what multi-human authority actually means here |
| AC2 identity & authority | `command-identity-unique` | **PASS** | client-generated command ids are unique and echoed back on the result |
| AC2 identity & authority | `retry-dedup` | **PASS** | the same command identity sent twice ran once and replayed the original result |
| AC1 shared control | `stale-turn-rejected` | **PASS** | a steer carrying a stale expectedTurnId is refused: expectedTurnId does not match the active turn |
| AC1 shared control | `steer-delivery-contract` | **PASS** | bob's steer landed inside alice's turn, attributed, with delivery=inline |
| AC2 identity & authority | `single-authoritative-approval` | **PASS** | two humans resolved the same approval at once; exactly one outcome was authoritative (accept by bob) |
| AC1 shared control | `identical-ordered-log` | **PASS** | both clients received the same 41 events in the same sequence order |
| AC1 shared control | `approval-gated-side-effect` | **PASS** | the approved command actually ran on the execution host after the human decision |
| AC2 identity & authority | `exactly-once-scope` | **RECORDED** | deduplication is relay-scoped: it makes a retried command identity run at most once through the hub. It is NOT a claim of exactly-once external side effects across a runtime crash mid-execution. |
| AC1 shared control | `real-turn-start[subscription]` | **PASS** | codex acknowledged thread 01a072cb-c799-7893-b00a-367de0ca5723 and completed the turn (completed) |
| AC1 shared control | `two-client-stream[subscription]` | **PASS** | both humans watched the same live Codex turn (14/13 events) |
| AC1 shared control | `steer-reaches-provider[subscription]` | **PASS** | the steer reached Codex as a resumed turn on session 01a072cb-c799-7893-b00a-367de0ca5723 (delivery=nextProviderTurn, provider echoed BETA: true) |
| AC1 shared control | `provider-resume[subscription]` | **PASS** | a second turn explicitly resumed Codex session 01a072cb-c799-7893-b00a-367de0ca5723 instead of starting a new one |
| AC1 shared control | `interrupt-during-tool-work[subscription]` | **PASS** | a teammate stopped Codex mid tool-work; the turn ended as interrupted and the provider process exited |
| AC1 shared control | `approval-during-codex-work[subscription]` | **BLOCKED** | `codex exec` runs its own commands inside its own sandbox and emits no approval request, so no teammate decision is possible for those commands |
| AC3 authentication | `auth-mode-exercised[subscription]` | **PASS** | lane subscription ran a real turn under auth mode "chatgpt" |
| AC1 shared control | `real-turn-start[api]` | **PASS** | codex acknowledged thread 01a072cc-31b4-74b1-8f26-bf4cbc892a12 and completed the turn (completed) |
| AC1 shared control | `two-client-stream[api]` | **PASS** | both humans watched the same live Codex turn (8/7 events) |
| AC1 shared control | `control-behaviours[api]` | **RECORDED** | steer, resume and interrupt were not re-run on this lane: they exercise provider-agnostic runtime code already proved on the subscription lane |
| AC3 authentication | `auth-mode-exercised[api]` | **PASS** | lane api ran a real turn under auth mode "apikey" |

`BLOCKED` is a capability this machine or this provider surface cannot supply. Each one carries a
concrete alternative in the JSON evidence. `RECORDED` rows are facts the ticket asks to be written
down rather than assertions that can pass or fail.
