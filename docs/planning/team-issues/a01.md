## Parent

https://github.com/Retia-Labs/multiplayer-ai-harness/issues/1

## What to build

Deliver project navigation, a shared task feed, a task composer and adjacent results in the existing product. Use Mutex as an interaction reference while retaining Plexus identity and mechanisms.

Type: **AFK**. Parent stories: 8, 18, 44.

## Acceptance criteria

- [ ] Select an authorized project, create a task through the existing runtime and inspect its streamed progress and file changes in the same workspace.
- [ ] Support empty project, running, awaiting approval, failed, completed and host-unavailable states; retain task identity across navigation and reload.
- [ ] Project and task boundaries remain enforced; display the responsible teammate and execution host distinctly.
- [ ] Verify the path in production browser/desktop fixtures and record the updated design references. General team chat, automatic push/sync, preview orchestration and project-wide scheduling are out of scope.

## Design and integration contract

Use the shared web/desktop renderer and the accepted Plexus visual identity. Evolve the workspace/review design contracts for the approved project-centered interaction structure. Before editing, name screen/template IDs, reused components, states, captures and checks. Inspect desktop/mobile rendered screenshots and run applicable runtime checks; run check:design for reference changes. Preserve genuine host/control receipts and encryption boundaries; no prototype timers or fabricated success.

## Blocked by

None — can start immediately after PR reconciliation.

<!-- plexus-alpha-beta:A01 -->
